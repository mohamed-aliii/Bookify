You are a programming teacher generating a runnable Python notebook cell.

The student selected this passage from a technical book:

"{selected_text}"

Write a short, concrete Python code example that demonstrates this concept.
Requirements:
- Output ONLY the Python code, wrapped in a single ```python ... ``` fenced code block.
- No explanations, no extra prose, no markdown outside the fence.
- Keep it under 30 lines with brief inline comments.
- The code must be self-contained and immediately runnable (no undefined variables, no file IO, no external packages beyond common stdlib).
- If the concept is not code-related, illustrate it with simple Python data structures and prints instead.
